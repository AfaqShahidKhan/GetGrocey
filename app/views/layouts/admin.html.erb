<!DOCTYPE html>
<html>
  <head>
    <title>GetGrossry</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-XXXXXX" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container mx-auto mt-28 px-5 flex">
    <nav class="flex w-full">
    <div class="w-64 p-4 h-screen bg-grey-600 sticky top-0">
      <div class="flex">
        <%= image_tag "logo.png", class: 'w-16' %>
        <h1>GetGrossry Admin</h1>
      </div>
      <ul class="flex flex-cool text-grey-50">
        <li>
          <%= link_to admin_path, class: "block px-4 py-2 hover:rounded hover:bg-grey-700" do %>
            <% icon('fa-solid', 'gauge-high')%><span class="hidden md:inline-block">Dashboard</span>
          <%end%>
        </li>
      </ul>
    </div>
    </nav>
      <%= yield %>
    </main>
  </body>
</html>
